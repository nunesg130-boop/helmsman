# Developer-only override. Production deployments use the GitHub Release's
# digest-pinned compose.yaml without this local build definition.
services:
  helmsman:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        HELMSMAN_VERSION: "1.0.5"
        HELMSMAN_REVISION: "local"
    image: "helmsman:1.0.5"
